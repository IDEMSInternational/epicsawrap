% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_to_database.R
\name{collate_station_metadata}
\alias{collate_station_metadata}
\title{Collate and standardise station metadata}
\usage{
collate_station_metadata(
  data,
  station_id = NULL,
  station_name = NULL,
  longitude = NULL,
  latitude = NULL,
  elevation = NULL,
  district = NULL,
  country_code = NULL
)
}
\arguments{
\item{data}{Data frame. The raw station metadata to process.}

\item{station_id}{Character or \code{NULL}. Name of the column in \code{data}
containing station IDs. Default \code{NULL}.}

\item{station_name}{Character or \code{NULL}. Name of the column in \code{data}
containing station names. Default \code{NULL}.}

\item{longitude}{Character or \code{NULL}. Name of the column in \code{data}
containing longitude values. Default \code{NULL}.}

\item{latitude}{Character or \code{NULL}. Name of the column in \code{data}
containing latitude values. Default \code{NULL}.}

\item{elevation}{Character or \code{NULL}. Name of the column in \code{data}
containing elevation values. Default \code{NULL}.}

\item{district}{Character or \code{NULL}. Name of the column in \code{data}
containing district names. Default \code{NULL}.}

\item{country_code}{Character or \code{NULL}. Name of the column in \code{data}
containing country codes. Default \code{NULL}.}
}
\value{
A data frame containing only the selected columns, renamed to the
standardised names, with two additional columns: \code{time_stamp} (UTC
datetime of when the function was called) and \code{status} (set to
\code{"active"} for all rows).
}
\description{
Selects and renames columns from a raw station metadata data frame into a
standardised format ready for export to the database. Adds a UTC timestamp
and sets a default \code{status} of \code{"active"} for all stations.
}
\note{
Only columns with a non-\code{NULL} mapping are selected and renamed.
Any columns in \code{data} not referenced by the parameters are dropped.
The output is intended to be passed to \code{export_to_database()} as the
\code{station_metadata} argument.
}
\examples{
raw <- data.frame(
  id    = c("ST01", "ST02"),
  name  = c("Station One", "Station Two"),
  lon   = c(32.1, 33.4),
  lat   = c(-1.2, -2.5),
  elev  = c(1100, 950),
  dist  = c("Northern", "Southern"),
  ccode = c("KE", "KE")
)

collate_station_metadata(
  data         = raw,
  station_id   = "id",
  station_name = "name",
  longitude    = "lon",
  latitude     = "lat",
  elevation    = "elev",
  district     = "dist",
  country_code = "ccode"
)

}
\seealso{
\code{\link{export_to_database}}
}
